import 'package:flutter/material.dart';

class StudentAttendence extends StatelessWidget {
  //const StudentHomework({Key? key}) : super(key: key);
  static const routeName = '/studentAttendence';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:Text('StudentAttendence')),
    );
  }
}
